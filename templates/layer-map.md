# Layer map

_Map the feature across AADM’s **five public delivery groupings** so reviews and evidence land in the right place. Roles are hats, not job titles._

Reference: [docs/udali-personas.md](../docs/udali-personas.md) (five groupings); optional [docs/udali-22-layer-model.md](../docs/udali-22-layer-model.md) (L1–L22).

---

## Feature / capability

<!-- Same name as PRD-lite / BIS-lite -->

## One-line summary

<!-- What changes in behavior or capability -->

---

## Layer mapping

Fill **what this feature touches** under each grouping: UI/UX, schemas, AUTH, services, shipping, etc. Use **None** with a short reason if truly untouched.

### Unifier

_UI safety, user experience, adapter (client/server) boundary clarity._

| Item | Notes |
|------|--------|
| Surfaces | <!-- screens, APIs exposed to clients --> |
| UX / safety | <!-- errors, destructive confirmations --> |
| Boundary clarity | <!-- what the client assumes vs server guarantees --> |

### Designer

_Schema, DTOs, domain logic, mapping._

| Item | Notes |
|------|--------|
| Contracts / schema | <!-- payloads, events --> |
| Domain rules | <!-- invariants, calculations --> |
| DTOs / mapping | <!-- boundaries between representations --> |

### Adapter

_Repositories, auth, authorization, caching._

| Item | Notes |
|------|--------|
| Data access | <!-- queries, transactions --> |
| AuthN | <!-- how identity is established for this flow --> |
| AuthZ | <!-- permission checks next to operations --> |
| Caching | <!-- if any; staleness considerations --> |

### Logician

_Services, workflows, orchestration, AI services, integrations._

| Item | Notes |
|------|--------|
| Application flow | <!-- orchestration steps --> |
| External systems | <!-- third-party calls --> |
| AI / automation | <!-- bounded AI steps, if any --> |

### Integrator (I)

_Controllers, routing, QA, deployment, integration validation—the **I** in **[UDALI](../docs/udali-personas.md)** means **Integrator**._

| Item | Notes |
|------|--------|
| Entrypoints | <!-- HTTP routes, consumers --> |
| QA evidence | <!-- tests / checks planned --> |
| Deploy / config | <!-- flags, env, rollout --> |
| Integration validation | <!-- staging checks against real deps --> |

---

## Cross-cutting risks

<!-- e.g. UI-only AUTH, hidden integration retries, agent-generated bypass -->

## Evidence plan

<!-- What proves each touched grouping (tests, review checklist, runbook). -->

---

## Example (fictional)

**Feature:** Weekly timesheet CSV export  

| Grouping | Touches |
|----------|---------|
| **Unifier** | Export button and week picker; clear errors for invalid range; client only requests export—does not assemble CSV |
| **Designer** | CSV column schema; domain rule “only own entries”; mapping from internal rows → CSV rows |
| **Adapter** | Repository read for entries; session/auth for member; authorize workspace membership before query; optional short-lived export cache **N/A** |
| **Logician** | Orchestrate validate → query → build file → audit; no external integrations |
| **Integrator** | `GET` or `POST` export route; integration tests for AUTH + CSV; deploy behind feature flag; validate in staging with prod-like data volume sample |

---

_Related: [bis-lite.md](bis-lite.md) §8._
